[project]
name = "hyperion"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tj5miniop", email = "82961157+tj5miniop@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "questionary>=2.1.1",
]

[project.scripts]
hyperion = "hyperion:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
